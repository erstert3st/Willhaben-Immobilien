python:
1: fix crawler
2: add real sql statements
        #also add create dynamic Table
3: handle already inserted willhabenIds
4: check if all picture urls in api
angular:
1: add sqlLite to Angular
2: add picture to Angular
   #add addPicture but only load if popup is open
3: edit sql dynamic on page 
4: check how deploy work 

docker: test after angular deploy
release alpha

Git Workflow:
  test workflow build angular deploy
 

Future me:
    Python:
        add interactive cli in python 
            make an fucking main 
            call python cli in angular
        add tests
        add const uppdater scrapper 
            #email notification
    Angular:
        add cordnate selector in angular
        add öffis maybe dynamisch
    Github:
        make workflow for ang& python & docker 
        make meme
        add Readme


done:
    python:
        make page crawker loop #90%
        make weird code better #everywhere i looked at :P
        add sqLite
        add all fields
        add sort and page handling in url 
    angular: 
        make map with service Marker and popup o.O
    Docker: 
        python+angular redaht container
     